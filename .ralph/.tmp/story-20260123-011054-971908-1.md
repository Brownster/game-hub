### TST-002: Add hub test helpers and Redis mock
Status: in_progress
Depends on: TST-001

Description:
Create test utilities for `services/hub` to stub Redis and control time (e.g., fake timers), enabling deterministic tests for room and Wordle daily logic.

Acceptance Criteria:
- [ ] A reusable in-memory Redis mock is added for `services/hub` tests.
- [ ] Tests can swap Redis without touching production code paths in most cases (minimal DI or module mocking).
- [ ] A helper exists to control time for date-based Wordle daily tests.
